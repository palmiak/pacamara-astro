---
import dateFormat, { masks } from "dateformat";
const {date} = Astro.props;
---
<small class=" text-tailblog-accent transition-all duration-300 text-[16px] dark:text-white/40">{ dateFormat(date, "dd-mm-yyyy") }</small>
